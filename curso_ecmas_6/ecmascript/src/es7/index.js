// Includes array
let numbers = [1, 2, 3, 7, 8];

if (numbers.includes(7)) {
  console.log("Yes, the number 7 is there ");
} else {
  console.log("No, is not there");
}

// Pow operator
let base = 4;
let exponent = 3;
let result = base ** exponent;
console.log(result);
